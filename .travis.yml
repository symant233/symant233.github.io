language: node_js
node_js: stable

# Travis-CI Caching
cache:
  directories:
    - node_modules

# S: Build Lifecycle
install:
  - npm install

before_script:
 # - npm install -g gulp
 - npm install hexo-cli --save
 - npm install hexo-renderer-jade hexo-renderer-stylus hexo-wordcount --save


script:
  # - npm audit fix
  - hexo clean && hexo g
  - cd ./public
  - git init
  - git config user.name "travis_bot"
  - git config user.email "recieve@outlook.jp"
  - git add .
  - git commit -m "Travis-CI auto build."
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master


# after_script:
#   - cd ../
#   - chmod +x ./bin/autoissue
#   - ./bin/autoissue
# E: Build LifeCycle

branches:
  only:
    - hexo
env:
 global:
   - GH_REF: github.com/symant233/symant233.github.io.git
